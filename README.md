# For a given array, finds the first contiguous subarray whose sum matches the given sum
# function subarray_sum($arr, $sum)
# Input: Array, Sum
# Output: comma separated indexes if found, -1 if not found
